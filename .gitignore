# Build artifacts
node_modules/
pkg/
target/
*.tmp

# Rendered documentation
/rendered/
/docs/rendered/
/site/

*.lock
!/Cargo.lock

# BL4 data files (will be distributed via GitHub Releases)
/share/*
# But keep manifest files compiled into bl4 library
!/share/manifest/category_names.json
!/share/manifest/parts_database.json
!/share/manifest/manufacturers.json
!/share/manifest/weapon_types.json

# Backup files
*.bak
*.bak.json

# Non-git hidden file patterns
.*
!.git*
