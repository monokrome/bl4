[package]
name = "bl4-preload"
version = "0.1.0"
edition = "2021"
description = "LD_PRELOAD library for intercepting RNG calls in Borderlands 4"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
once_cell = "1.19"
backtrace = "0.3"
ctor = "0.2"

[profile.release]
opt-level = 3
lto = true
