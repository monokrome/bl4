{
  "name": "bl4-save-editor-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ../../bl4 --target web --features wasm && cp ../../bl4/pkg/* src/wasm/",
    "dev": "pnpm wasm && vite",
    "build": "pnpm wasm && vite build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "bellagonia": "^0.3.0",
    "gonia": "^0.4.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "vite": "^6"
  }
}
